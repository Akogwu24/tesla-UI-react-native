import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    width: '100%',
    alignItems: 'center',
    marginTop: 15,
    borderRadius: 15,
  },
  btnText: {
    fontSize: 24,
    padding: 5,
  },
});
export default styles;
